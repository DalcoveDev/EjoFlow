import { LogOut, Settings, ShieldCheck, UserRound } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import { useAuth } from '../auth/AuthContext';
import { langNames, useI18n, type Lang } from '../i18n/LanguageContext';
export function ProfilePage(){const { user, logout } = useAuth(); const navigate = useNavigate(); const { t, lang, setLang } = useI18n(); const name = user?.name ?? t('ui.user'); const initials = user?.initials ?? 'U'; return <main className="mx-auto max-w-2xl px-4 py-8 md:px-10 md:py-12"><p className="eyebrow">{t('login.formEyebrow')}</p><h2 className="font-display text-2xl font-bold">{name}</h2><p className="mt-2 text-sm text-ejo-muted">{t('profile.subtitle')}</p><section className="mt-7 border border-ejo-border bg-white"><div className="flex items-center gap-3 border-b border-ejo-border p-5"><span className="grid size-11 place-items-center rounded-full bg-[#dcece5] text-ejo-green"><UserRound size={21}/></span><div><strong className="block font-display text-base">{name}</strong><small className="text-xs text-ejo-muted">{t('ui.language')}: {langNames[lang]}</small></div></div><div className="flex gap-3 border-b border-ejo-border p-5"><ShieldCheck className="shrink-0 text-ejo-green" size={21}/><p className="text-xs leading-5 text-ejo-muted"><strong className="block text-ejo-ink">{t('profile.security')}</strong>{t('profile.securityText')}</p></div><div className="border-b border-ejo-border p-5"><p className="mb-2 text-[10px] font-bold text-ejo-muted">{t('ui.language')}</p><div className="flex flex-wrap gap-2">{(Object.keys(langNames) as Lang[]).map(l => <button key={l} onClick={() => setLang(l)} className={`border px-4 py-2 text-xs font-bold ${lang === l ? 'border-ejo-green bg-[#f1f7f4] text-ejo-green' : 'border-ejo-border bg-white text-ejo-blue hover:border-ejo-green'}`}>{langNames[l]}</button>)}</div></div><div className="border-b border-ejo-border p-5"><button onClick={() => navigate('/app/igenamiterere')} className="flex w-full items-center justify-center gap-2 border border-ejo-border px-4 py-3 text-xs font-bold text-ejo-blue hover:border-ejo-green">{t('title.settings')} <Settings size={14}/></button></div><div className="p-5"><button onClick={() => { logout(); navigate('/injira', { replace: true }); }} className="flex w-full items-center justify-center gap-2 border border-ejo-blue px-4 py-3 text-xs font-bold text-ejo-blue hover:bg-[#f1f7f4]">{t('ui.logout')} <LogOut size={14}/></button></div></section></main>}