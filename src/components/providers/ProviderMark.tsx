export function ProviderMark({ mark, id, size }: { mark: string; id: string; size?: 'sm' | 'md' | 'lg' }) { const tone = id === 'irembo' ? 'rounded-sm bg-ejo-blue' : id === 'rra' ? 'rounded-full bg-[#485d42]' : 'rounded-sm bg-[#705342]'; const sizing = size === 'lg' ? 'size-14 text-3xl' : size === 'md' ? 'size-11 text-2xl' : 'size-9 text-lg'; return <span className={`grid shrink-0 place-items-center ${tone} ${sizing} font-display font-extrabold text-white`}>{mark}</span> }